import * as styles from "./styles";
import theme from "./theme";
import { Routes } from "./routes";
import { validateName, validateEmail, isEmpty } from "./validators";

export default { styles, theme, Routes, validateName, validateEmail, isEmpty };
